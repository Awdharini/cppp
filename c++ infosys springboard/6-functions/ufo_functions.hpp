#include <vector>

void display_misses(int misses);

// Declare functions
void greet();
void display_status(std::vector<char> incorrect,char answer);
void end_game(char answer,char codeword);
